# FrED (Raspberry Pi) Python requirements, installed into the venv.
#
# The venv is created with --system-site-packages, but the hardware stack is
# also pinned here so the venv works even when the system copies live in a
# place the venv can't see (e.g. a --user install under ~/.local).
#
# These provide the modules experiment_runner.py imports:
#   board / busio / digitalio      -> Adafruit-Blinka
#   adafruit_mcp3xxx               -> adafruit-circuitpython-mcp3xxx
#   RPi.GPIO                       -> RPi.GPIO
#   spidev                         -> spidev
Adafruit-Blinka
adafruit-circuitpython-mcp3xxx
RPi.GPIO
spidev
numpy
