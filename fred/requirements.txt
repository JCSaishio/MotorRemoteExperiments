# Python packages installed by pip INTO the fred-venv virtual environment.
#
# NOTE: RPi.GPIO is intentionally NOT listed here. On a Raspberry Pi it is
# installed from apt (python3-rpi.gpio) by setup_install.sh and made visible to
# the venv via --system-site-packages. Pip-installing RPi.GPIO into the venv can
# SHADOW the working apt build and stop the motor from being driven, so we use
# the apt version instead (same approach as the main fred-device framework).

# --- Math ---
numpy

# --- Pi hardware: CircuitPython layer (SPI/pins) + MCP3008 ADC + SPI ---
adafruit-blinka
adafruit-circuitpython-mcp3xxx
spidev
