# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
fred-venv/

# Reference project (kept locally, not part of this repo)
fred-device-external-cv-pi4v6/

# Excel files are experiment data (inputs, results, previews, lock files) and
# stay out of the repo. Example.xlsx is the one tracked exception (sample input).
*.xlsx
!Example.xlsx

# OS
.DS_Store
Thumbs.db

# Editor / tooling
.claude/
