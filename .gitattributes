# Force LF for scripts that run on the Raspberry Pi (Linux), so cloning from
# Windows never introduces CRLF that would break the shebang/interpreter.
*.sh   text eol=lf
*.py   text eol=lf

# Windows batch launchers must stay CRLF
*.bat  text eol=crlf
